# Working notes

Student drafts live here. One idea per file. Name files `YYYY-MM-DD-short-slug.md`.

This is **not** the research wiki. When a note is worth keeping as compiled memory, drop a copy or pointer in `RESEARCH/raw/` and ingest it. Meeting minutes go in `docs/meetings/`, not here.

Put `needs-SME-review` on any biological claim that did not come from Dr. Krolewski or a paper on the starter list.

Do not put API keys or identifiable data in these notes.
